#:kivy 2.1.0

<MainWindow>:
    label:label
    actorName:actorInput

    canvas:
        Rectangle:
            size: self.size
            pos: self.pos

    FloatLayout:
        TextInput:
            id: actorInput
            pos_hint:{"x": 0.3, "top":0.9}
            size_hint:0.35, 0.05
            hint_text: "Search..."
            multiline: False
            on_text:root.actorInputCallback()

        Label:
            id: label
            pos_hint:{"x": 0.1, "top":0.9}
            size_hint:0.8, 0.2
            text: "saucisse²"
